import React from 'react'

export default function HowInstall() {
    return (
        <div style={{color: "#606060", backgroundColor: "beige", width: "100%", fontSize: "20px", fontFamily: "Arial"}}>
            For Lab 10 part C, I decided to learn to use React as a framework since I know that it is one of the more popular frameworks when it comes to web dev. In order to install react on my computer, I had to first install node.js, them used several npm commands to create a react project, which came with several files that contributed to a prebuilt default application. 
        </div>
    )
}